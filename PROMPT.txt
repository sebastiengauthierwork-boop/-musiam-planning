URGENT : le superadmin a MOINS de droits qu'avant car partout le code verifie role === 'admin' mais mon role est 'superadmin'.

Faire un remplacement GLOBAL dans TOUT le code :

1. Chercher dans TOUS les fichiers .tsx et .ts du projet TOUTES les occurrences de :
   - role === 'admin'
   - role == 'admin'
   - role !== 'admin'
   - role != 'admin'
   - role === "admin"
   - role == "admin"
   - .includes('admin')
   - IN ('admin'

2. Pour CHAQUE occurrence, ajouter 'superadmin' :
   - role === 'admin' -> role === 'admin' || role === 'superadmin'
   - role === "admin" -> role === "admin" || role === "superadmin"
   - ['admin'].includes(role) -> ['admin', 'superadmin'].includes(role)
   - role IN ('admin', ...) -> role IN ('admin', 'superadmin', ...)

3. La regle : PARTOUT ou un admin a un droit, le superadmin a le MEME droit PLUS les droits exclusifs du superadmin.

4. Verifier aussi les politiques RLS dans le code qui generent du SQL avec 'admin'

5. Utiliser grep -r "admin" pour trouver toutes les occurrences

git add . && git commit -m "fix: superadmin tous droits admin" && git push origin main