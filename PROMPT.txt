Ajout du choix de couleur dans les parametres des codes horaires et codes absence :

1. COLONNES EN BASE : genere le SQL et copie avec clip :
ALTER TABLE shift_codes ADD COLUMN IF NOT EXISTS color VARCHAR(7);
ALTER TABLE absence_codes ADD COLUMN IF NOT EXISTS color VARCHAR(7);

2. FORMULAIRE CODES HORAIRES (parametrage) :
- Ajouter un champ "Couleur" avec un color picker (input type="color")
- A cote du color picker, afficher un apercu du code avec la couleur choisie (fond = couleur, texte = noir ou blanc selon contraste)
- Quand une couleur est selectionnee, verifier si cette couleur est deja utilisee par un autre code du meme site
- Si oui, afficher un message orange : "Cette couleur est deja utilisee par le code [CODE] ([LABEL]). Voulez-vous quand meme l'utiliser ?"
- Deux boutons : "Changer de couleur" et "Utiliser quand meme"
- Ne PAS bloquer la sauvegarde, c'est juste informatif

3. FORMULAIRE CODES ABSENCE (parametrage) :
- Meme logique : color picker + verification doublon

4. FONCTION getCodeColor MISE A JOUR :
- Dans lib/utils.ts, modifier getCodeColor pour :
  a) D'abord chercher si le code a une couleur personnalisee en base (colonne color)
  b) Si oui, utiliser cette couleur
  c) Si non, utiliser la couleur par defaut calculee par la nomenclature (M=bleu, E=vert, etc.)
- Passer les shift_codes et absence_codes avec leur couleur au composant pour eviter une requete supplementaire

5. Les couleurs personnalisees s'appliquent sur TOUTES les pages (saisie, planning imprimable, feuille emargement, compteur, feuille du jour, mon-planning)

git add . && git commit -m "feat: couleur personnalisable par code horaire" && git push origin main