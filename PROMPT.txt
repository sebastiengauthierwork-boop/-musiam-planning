Bug "Acces refuse" sur /api/create-user :

L'API retourne "Acces refuse" quand un admin essaie de creer un utilisateur. Le probleme vient probablement de la verification du role de l'appelant.

Debugger :
1. Dans app/api/create-user/route.ts, ajouter des console.log pour voir :
   - Le token recu
   - Le user recupere depuis le token
   - Le role du user
   - La raison exacte du refus
2. Verifier que la verification du token utilise bien la anon key pour decoder (pas la service_role key)
3. Verifier que le role est bien lu depuis la table users (pas depuis auth.users qui n'a pas de champ role)
4. S'assurer que la route accepte les roles 'admin' ET 'responsable'
5. Tester avec un console.log en tout debut de route pour verifier que la route est bien atteinte

git add . && git commit -m "fix: debug acces refuse create-user" && git push origin main